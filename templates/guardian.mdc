---
description: 🛡️ PROJECT SNAPSHOT - Read before ANY action
globs: **/*
alwaysApply: true
---

# 🧠 PROJECT SNAPSHOT
> ⚠️ READ THIS BEFORE ANY CODE CHANGE
> Auto-synced: {{TIMESTAMP}}

---

## IDENTITY
```yaml
name: {{PROJECT_NAME}}
purpose: {{ONE_LINE_PURPOSE}}
status: {{development|staging|production}}
```

---

## TECH_STACK
> ❌ DO NOT SUGGEST ALTERNATIVES
```yaml
frontend: {{FRAMEWORK}}
backend: {{FRAMEWORK}}
database: {{DATABASE}}
styling: {{CSS_METHOD}}
# Add more as needed
```

---

## DEPENDENCIES
```yaml
frontend:
  {{package}}: {{version}}
  {{package}}: {{version}}
backend:
  {{package}}: {{version}}
  {{package}}: {{version}}
```

---

## ENV_VARS
```yaml
required:
  - {{VAR_NAME}}: {{description}}
  - {{VAR_NAME}}: {{description}}
optional:
  - {{VAR_NAME}}: {{description}}
```

---

## FILES
> 📂 CHECK HERE BEFORE CREATING ANY FILE
> Format: `path: purpose | main_functions`

### Frontend
```
src/components/{{Component}}.jsx: {{purpose}} | {{func1}}, {{func2}}
src/hooks/{{useHook}}.js: {{purpose}} | {{func1}}, {{func2}}
src/pages/{{Page}}.jsx: {{purpose}} | -
src/lib/{{util}}.js: {{purpose}} | {{func1}}, {{func2}}
```

### Backend
```
api/main.py: server-entry | app, {{handlers}}
api/routes/{{route}}.py: {{purpose}} | {{endpoints}}
api/services/{{service}}.py: {{purpose}} | {{functions}}
api/models/{{model}}.py: {{purpose}} | {{classes}}
```

### Config
```
package.json: npm config
{{config_file}}: {{purpose}}
```

---

## CONNECTIONS
```yaml
frontend:
  port: {{PORT}}
  connects_to: [backend]
backend:
  port: {{PORT}}  # ⚠️ DO NOT CHANGE
  connects_to: [database, {{external_apis}}]
websocket:
  path: {{/ws}}
  used_by: [{{files}}]
database:
  type: {{SQLite|PostgreSQL|etc}}
  path: {{path_if_local}}
```

---

## RUN
> ⚡ USE EXACTLY THESE COMMANDS
```bash
# Development
frontend: {{command}}
backend: {{command}}

# Test
test_frontend: {{command}}
test_backend: {{command}}
test_e2e: {{command}}
```

---

## LOCKED
> 🔒 CANNOT CHANGE WITHOUT USER APPROVAL
```yaml
- {{decision}}: {{value}} # locked: {{date}} | {{reason}}
- {{decision}}: {{value}} # locked: PERMANENT
```

---

## DANGER
> ⚠️ THESE FILES BREAK EASILY
```yaml
- path: {{file}}, reason: {{why_dangerous}}
- path: {{file}}, reason: {{why_dangerous}}
```

---

## ISSUES
> 🐛 CURRENT KNOWN ISSUES
```yaml
1. {{description}} | {{related_file}}
2. {{description}} | {{related_file}}
```

---

## CHANGES
> 📝 RECENT CHANGES (Last 10)
```yaml
- {{YYYY-MM-DD}}: {{description}} | {{files}}
- {{YYYY-MM-DD}}: {{description}} | {{files}}
```

---

## AGENT_RULES
> 🤖 YOU MUST FOLLOW THESE

### Before ANY Action:
1. ✅ Read this snapshot
2. ✅ Check FILES - does similar exist?
3. ✅ Check LOCKED - am I violating any?
4. ✅ Check DANGER - is this risky?

### Before Creating File:
- CHECK: FILES section for same purpose
- IF EXISTS: Ask "تعديل الموجود أم إنشاء جديد؟"
- LOCATION: Follow existing folder pattern

### Before Modifying Code:
- CHECK: CONNECTIONS - what depends on this?
- CHECK: DANGER - is this file risky?
- WARN: If touching danger zone

### After ANY Change:
- UPDATE: FILES if new file created
- UPDATE: CHANGES section
- UPDATE: ISSUES if bug fixed

---

## THINKING_RULES
> 🧠 HOW TO THINK BEFORE ACTING
> 📖 [WORKFLOW.md](docs/WORKFLOW.md) | [CODE_PRINCIPLES.md](docs/CODE_PRINCIPLES.md) | [THINKING_PATTERNS.md](docs/THINKING_PATTERNS.md)

### 1. Understand Before Solving
```
❌ WRONG: Jump to solution immediately
✅ RIGHT: Read error → Understand flow → Then solve
```

### 2. Test Before Saying "Done"
```
❌ WRONG: "تم الإصلاح" without testing
✅ RIGHT: Run code → Verify output → Then confirm
```

### 3. Follow Full Flow
```
❌ WRONG: Fix one file, ignore dependencies
✅ RIGHT: Trace: Input → Processing → Output → Verify each step
```

### 4. Never Assume - Verify
```
❌ WRONG: "The database should have this data"
✅ RIGHT: Query database directly → Show actual data
```

### 5. Check Before Creating
```
❌ WRONG: Create new file immediately
✅ RIGHT: Search existing files → Maybe it exists → Then decide
```

### 6. Show Your Work
```
❌ WRONG: "I fixed it"
✅ RIGHT: "I fixed it, here's the output: [actual result]"
```

### 7. One Change at a Time
```
❌ WRONG: Change 5 things at once
✅ RIGHT: Change one thing → Test → Confirm → Next
```

### 8. Undo-able Changes
```
❌ WRONG: Delete/overwrite without backup
✅ RIGHT: Git commit first OR backup file
```

### 9. Read Error Messages
```
❌ WRONG: Ignore stack trace, guess the problem
✅ RIGHT: Read line number → Read function → Understand context
```

### 10. Ask When Confused
```
❌ WRONG: Assume user intent
✅ RIGHT: "هل تقصد X أم Y؟"
```
