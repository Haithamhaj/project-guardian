# 🛡️ دليل Guardian-H المحسّن v7.0

## 🌟 الجديد في الإصدار 7.0

Guardian-H v7.0 يقدم **نظام ذاكرة وإدارة قرارات متقدم للوكلاء الذكيين** - حل شامل للـ 26 مشكلة التي تم تحديدها في العمل مع وكلاء البرمجة بالذكاء الاصطناعي.

### 🎯 الميزات الأساسية

1. **🧠 نظام الذاكرة المحسّن**
   - ذاكرة دائمة عبر الجلسات
   - تتبع وقفل القرارات
   - سجل التغييرات مع التحقق
   - نسخ احتياطية للمشروع مع الإصدارات
   - مراقبة الصحة

2. **🎯 نظام دعم القرارات**
   - توصيات التقنيات
   - كشف التعارضات
   - آلية قفل القرارات
   - فحص التوافق

3. **🔍 نظام مراقبة الجودة**
   - كشف الكود الميت
   - مكتشف الملفات المكررة
   - فرض البنية
   - تتبع نشاط الملفات
   - تقييم الجودة

4. **📊 مراقبة الصحة**
   - صحة المشروع الفورية
   - كشف تعب الوكيل
   - تتبع التحقق
   - محرك التوصيات

---

## 🚀 التثبيت

### الخيار 1: npx (موصى به)
```bash
# تشغيل النسخة المحسنة
npx guardian-h@latest

# أو استخدام الأمر المحسّن مباشرة
npx guardian-enhanced
```

### الخيار 2: التثبيت العالمي
```bash
npm install -g guardian-h
guardian-enhanced
```

### الخيار 3: Python المباشر (الميزات الكاملة)
```bash
# نسخ أو تحميل
git clone https://github.com/Haithamhaj/guardian-h
cd guardian-h

# تشغيل الماسح المحسّن
python3 src/guardian_enhanced.py /path/to/your/project
```

---

## 📚 دليل الاستخدام

### الاستخدام الأساسي

```bash
# مسح المجلد الحالي
guardian-enhanced

# مسح مشروع محدد
guardian-enhanced ./my-project

# إخراج مخصص
guardian-enhanced -o custom.mdc

# تخطي فحص الجودة (أسرع)
guardian-enhanced --no-quality
```

### الاستخدام المتقدم

#### 1. قفل قرار تقني

```bash
# قفل React كإطار عمل للواجهة الأمامية
guardian-enhanced --lock-decision React frontend_framework "الفريق لديه خبرة في React"
```

أو برمجياً:

```python
from src.guardian_enhanced import GuardianEnhanced

guardian = GuardianEnhanced("./my-project")
guardian.lock_tech_decision(
    tech_name="React",
    category="frontend_framework",
    reasoning="الفريق لديه 3 سنوات خبرة في React",
    status="PERMANENT"
)
```

#### 2. فحص تعارض المقترح

```bash
# فحص إذا كان التبديل إلى Vue يتعارض
guardian-enhanced --check-proposal "التبديل إلى Vue بدلاً من React"
```

الناتج:
```
⛔ لا يمكن المتابعة - يتعارض مع قرار (قرارات) مقفل. افتح القفل أولاً.
```

#### 3. الحصول على توصيات التقنيات

```python
from src.guardian_enhanced import GuardianEnhanced

guardian = GuardianEnhanced("./my-project")
recommendations = guardian.get_tech_recommendation(
    category="frontend",
    project_type="web application",
    team_experience="beginner"
)

for rec in recommendations['recommendations']:
    print(f"{rec['name']}: النقاط {rec['score']}")
```

#### 4. تشغيل فحص الجودة

```python
from src.quality_control import QualityController

qc = QualityController("./my-project")
report = qc.run_full_scan()

print(f"نتائج الكود الميت: {report['dead_code']['findings_count']}")
print(f"التكرارات: {report['duplicates']['count']}")
print(f"نقاط الصحة: {report['summary']['health_score']}/100")
```

---

## 🗂️ بنية ملف Guardian المحسّن

النسخة المحسنة تنشئ مجلد `.guardian` مع بيانات دائمة:

```
.guardian/
├── decisions.json      # القرارات التقنية المقفلة
├── changes.json        # سجل التغييرات
├── sessions.json       # جلسات الوكيل الذكي
└── snapshots/          # نسخ احتياطية للمشروع
    ├── snapshot_20241208_140530.json
    └── snapshot_20241208_141215.json
```

---

## 📋 تنسيق MDC المحسّن

الملف المولّد `guardian_enhanced.mdc` يتضمن أقسام جديدة:

```yaml
## 🔒 LOCKED_DECISIONS (القرارات المقفلة)
- category: frontend_framework
  decision: React as frontend_framework
  status: PERMANENT
  reasoning: الفريق لديه خبرة في React
  locked_at: 2024-12-08T14:05:30

## 📝 CHANGE_HISTORY (سجل التغييرات)
- timestamp: 2024-12-08T14:10:15
  type: FILE_CREATED
  description: تم إنشاء App.jsx
  verification: verified

## 💊 HEALTH_STATUS (حالة الصحة)
status: healthy
score: 95/100
locked_decisions: 3
total_changes: 12
unverified_changes: 0

## 🔍 QUALITY_REPORT (تقرير الجودة)
code_health: 88/100
dead_code_findings: 2
duplicate_files: 1
structure_violations: 3

## 💡 RECOMMENDATIONS (التوصيات)
- جميع الأنظمة صحية ✅
```

---

## 🤖 كيف تستخدم الوكلاء الذكيين Guardian المحسّن

### قبل إجراء التغييرات

```python
# الوكيل يفحص المقترح مقابل القرارات المقفلة
result = guardian.check_proposal("التبديل من React إلى Vue")

if result['has_conflicts']:
    # الوكيل يتوقف ويسأل المستخدم
    print(f"⛔ {result['recommendation']}")
    # يُظهر أي قرار يتعارض ولماذا
else:
    # الوكيل يتابع بأمان
    print(f"✅ {result['recommendation']}")
```

### بعد إجراء التغييرات

```python
# الوكيل يسجل التغيير
guardian.memory.log_change(
    ChangeType.FILE_CREATED,
    "تم إنشاء مكون UserProfile",
    ["src/components/UserProfile.jsx"],
    "ينفذ ميزة عرض ملف المستخدم"
)

# لاحقاً، وضع علامة كمُحقق
guardian.memory.verify_change(change_id, "verified")
```

### تتبع الجلسة

```python
# بدء الجلسة
session = guardian.memory.start_session("cursor")

# التحديث بالمهام
guardian.memory.update_session(
    session.session_id,
    task="تمت إضافة مصادقة المستخدم",
    warning=False,  # لا تحذيرات
    error=False     # لا أخطاء
)

# الحصول على تقرير الصحة
health = guardian.memory.get_health_report()
if health['status'] == 'concerning':
    print("⚠️ فكّر في بدء جلسة جديدة")
```

---

## 🎯 حل الـ 26 نقطة ألم

### مشاكل الذاكرة والسياق (4 مشاكل)

✅ **الحل: نظام الذاكرة المحسّن**
- قرارات دائمة عبر الجلسات
- تتبع الجلسة مع السياق
- إصدارات النسخ الاحتياطية
- لا يفقد السياق أبداً

### مشاكل الملفات والبنية (5 مشاكل)

✅ **الحل: نظام مراقبة الجودة**
- كاشف الكود الميت يجد الملفات غير المستخدمة
- مكتشف التكرارات يمنع التكرار
- سجل الملفات يتتبع الملفات النشطة
- فارض البنية يحافظ على التنظيم

### مشاكل التحقق والثقة (3 مشاكل)

✅ **الحل: طبقة التحقق**
- سجل التغييرات مع حالة التحقق
- مراقبة الصحة تكتشف تعب الوكيل
- توصيات للتعافي
- فحص الصدق من خلال التحقق

### مشاكل القرارات التقنية (4 مشاكل)

✅ **الحل: نظام دعم القرارات**
- مستشار التقنية يوفر السياق
- قفل القرارات يمنع التعارضات
- حلّال التعارضات يمسك التناقضات
- فحص التوافق

### مشاكل التوثيق (4 مشاكل)

✅ **الحل: التوثيق التلقائي**
- MDC محسّن مع كل الأقسام
- مصدر واحد للحقيقة
- تنسيق متسق
- مُزامن تلقائياً

### مشاكل المحادثة الطويلة (3 مشاكل)

✅ **الحل: إدارة الجلسة**
- كشف التعب
- تقييم صحة الجلسة
- توصيات للبدء من جديد
- حفظ السياق

### مشاكل التعديلات (3 مشاكل)

✅ **الحل: تتبع التغييرات**
- منع تأثير الفراشة
- تتبع الآثار الجانبية
- تحليل التأثير
- دعم التراجع عبر النسخ الاحتياطية

---

## 🔧 التكوين

إنشاء `.guardian/config.json` للإعدادات المخصصة:

```json
{
  "quality_control": {
    "max_file_lines": 500,
    "max_function_lines": 100,
    "similarity_threshold": 0.8
  },
  "health": {
    "warning_threshold": 70,
    "critical_threshold": 50
  },
  "decisions": {
    "auto_lock_tech_stack": true,
    "require_approval_for_unlock": true
  }
}
```

---

## 💡 أفضل الممارسات

### 1. قفل القرارات الحرجة مبكراً

```python
# قفل حزمة التقنيات بعد الإعداد الأولي
guardian.lock_tech_decision("React", "frontend", "قرار الفريق", "PERMANENT")
guardian.lock_tech_decision("FastAPI", "backend", "قرار الفريق", "PERMANENT")
guardian.lock_tech_decision("PostgreSQL", "database", "متطلبات المشروع", "PERMANENT")
```

### 2. تشغيل فحوصات الجودة بانتظام

```bash
# فحص جودة أسبوعي
guardian-enhanced --output weekly_report.mdc
```

### 3. مراقبة الصحة

```python
health = guardian.memory.get_health_report()

if health['health_score'] < 70:
    print("⚠️ تحذير: صحة المشروع تتراجع")
    for rec in health['recommendations']:
        print(f"  - {rec}")
```

### 4. مراجعة التغييرات

```python
# الحصول على التغييرات الأخيرة
changes = guardian.memory.get_changes(limit=10)

# التحقق من التغييرات المهمة
for change in changes:
    if change.verification_status == "unverified":
        # اختبار التغيير
        # ثم وضع علامة كمُحقق
        guardian.memory.verify_change(change.id, "verified")
```

### 5. بدء جلسات جديدة

عندما يبدو الوكيل "متعباً" (يرتكب أخطاء متكررة):

```python
health = guardian.memory.get_health_report()
if health['status'] == 'concerning':
    # ابدأ محادثة/جلسة جديدة مع الوكيل
    # السياق السابق محفوظ في .guardian/
```

---

## 🧪 الاختبار

تشغيل مجموعة الاختبار:

```bash
# تشغيل جميع الاختبارات
python3 tests/test_enhanced.py

# تشغيل اختبار محدد
python3 tests/test_enhanced.py TestMemoryManager
```

يجب أن تنجح جميع الاختبارات الـ 25:
- مدير الذاكرة: 6 اختبارات
- مستشار التقنية: 4 اختبارات
- حلّال التعارضات: 2 اختبار
- مراقبة الجودة: 5 اختبارات
- Guardian المحسّن: 5 اختبارات
- التكامل: 3 اختبارات

---

## 🌍 تكامل IDE

Guardian المحسّن يعمل مع جميع بيئات تطوير الذكاء الاصطناعي الرئيسية:

### Cursor
الملفات تُحفظ في: `.cursor/rules/guardian.mdc`

### Windsurf
الملفات تُحفظ في: `.windsurf/rules/guardian.md`

### GitHub Copilot
الملفات تُحفظ في: `.github/copilot-instructions.md`

### Claude Code
الملفات تُحفظ في: `CLAUDE.md`

---

## 🚧 الانتقال من v6 إلى v7

### الانتقال التلقائي

```bash
# v7 تقرأ ملفات v6 تلقائياً
guardian-enhanced
```

### الخطوات اليدوية

1. **نسخ احتياطي لـ guardian.mdc الموجود**
   ```bash
   cp guardian.mdc guardian.mdc.v6.backup
   ```

2. **تشغيل المسح المحسّن**
   ```bash
   guardian-enhanced
   ```

3. **قفل القرارات التقنية الموجودة**
   ```python
   guardian = GuardianEnhanced(".")
   # قفل حزمة التقنيات الخاصة بك
   guardian.lock_tech_decision("React", "frontend", "الاختيار الموجود", "PERMANENT")
   ```

4. **مراجعة الملف المولّد**
   تحقق من مجلد `.guardian/` والأقسام الجديدة في MDC

---

## 🤝 المساهمة

راجع [CONTRIBUTING.md](CONTRIBUTING.md) للإرشادات.

---

## 📄 الترخيص

ترخيص MIT - راجع [LICENSE](LICENSE)

---

## 🙏 الاعتمادات

صُنع بـ ❤️ بواسطة [هيثم](https://github.com/Haithamhaj) والمجتمع

شكر خاص لجميع المساهمين الذين ساعدوا في تحديد وحل الـ 26 نقطة ألم!

---

## 📞 الدعم

- GitHub Issues: [الإبلاغ عن الأخطاء](https://github.com/Haithamhaj/guardian-h/issues)
- المناقشات: [طرح الأسئلة](https://github.com/Haithamhaj/guardian-h/discussions)
- Twitter: [@Haithamhaj](https://twitter.com/Haithamhaj)

---

صُنع بـ 💡 بواسطة [Imperfect Success](https://imperfectsuccess.com)
