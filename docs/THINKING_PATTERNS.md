# 🧠 Guardian Thinking Patterns

> **أنماط التفكير لحل المشاكل البرمجية**
> استخدم النمط المناسب حسب نوع المشكلة

---

## 🔍 للتحليل والفهم

### 1. Divide & Conquer (فرّق تَسُد)
```
المشكلة الكبيرة → أجزاء صغيرة → حل كل جزء → دمج

متى تُستخدم: ميزة جديدة كبيرة، مهمة معقدة
```

**مثال:**
```
"بناء نظام تسجيل دخول"
↓
1. UI (form + validation)
2. API (endpoint)
3. Auth logic (verify + token)
4. Database (user storage)
↓
حل كل جزء منفصل → دمج
```

---

### 2. 5 Whys (لماذا × 5)
```
لماذا؟ → لماذا؟ → لماذا؟ → لماذا؟ → لماذا؟ → السبب الجذري

متى تُستخدم: خطأ متكرر، مشكلة عميقة
```

**مثال:**
```
❌ التطبيق بطيء
  → لماذا؟ الـ API بطيء
    → لماذا؟ الـ query بطيء
      → لماذا؟ لا يوجد index
        → لماذا؟ لم نُضف عند التصميم
          → لماذا؟ لم نتوقع هذا الحجم
✅ الحل: إضافة index + مراجعة التصميم للمستقبل
```

---

### 3. Rubber Duck Debugging
```
اشرح الكود سطر بسطر بصوت عالٍ → ستجد المشكلة بنفسك

متى تُستخدم: أخطاء غامضة، كود "المفترض يشتغل"
```

**كيف تعمل:**
```
1. خذ "بطة" (أو أي شيء)
2. اشرح لها ما المفترض أن يفعله الكود
3. اشرح ما يفعله فعلياً سطر بسطر
4. عندما تجد الفرق = وجدت المشكلة
```

---

### 4. Binary Search Debugging
```
المشكلة في أي نصف؟ → ضيّق → كرر

متى تُستخدم: خطأ في ملف كبير، لا تعرف أين
```

**مثال:**
```
الملف 1000 سطر، خطأ في مكان ما
↓
أضف log عند السطر 500 → يظهر طبيعي؟
↓
نعم → المشكلة بعد 500
↓
أضف log عند 750 → يظهر طبيعي؟
↓
لا → المشكلة بين 500-750
↓
كرر حتى تجدها
```

---

## 💡 لإيجاد الحلول

### 5. First Principles (المبادئ الأولى)
```
ما الهدف الأصلي؟ → تجاهل الافتراضات → ابنِ من الصفر

متى تُستخدم: حل غير تقليدي، إعادة تصميم
```

**مثال:**
```
المشكلة: "نحتاج cache أفضل"
↓
First Principles:
- ما الذي نحتاج تخزينه؟
- لماذا نحتاج cache أصلاً؟
- ماذا لو غيّرنا architecture؟
↓
ربما الحل ليس cache أفضل، بل تصميم مختلف
```

---

### 6. Inversion (التفكير العكسي)
```
بدلاً من "كيف أنجح؟" → "كيف أفشل؟" → تجنب أسباب الفشل

متى تُستخدم: تصميم آمن، منع الأخطاء
```

**مثال:**
```
الهدف: "تصميم API آمن"
↓
Inversion: "كيف يُخترق الـ API؟"
- لا يوجد rate limiting
- لا يوجد input validation
- Tokens لا تنتهي
↓
الحل: أضف كل ما يمنع الاختراق
```

---

### 7. Occam's Razor (أبسط تفسير)
```
إذا هناك عدة تفسيرات → اختر الأبسط أولاً

متى تُستخدم: التخمين بين أسباب متعددة
```

**مثال:**
```
المشكلة: "الـ form لا يُرسل البيانات"
↓
تفسيرات محتملة:
1. ❓ مشكلة في الـ CORS
2. ❓ الـ backend معطل
3. ✅ زر Submit لا يفعل شيء (الأبسط)
↓
ابدأ بالتحقق من الأبسط أولاً
```

---

### 8. Pattern Matching
```
هل رأيت هذه المشكلة من قبل؟ → استخدم نفس الحل

متى تُستخدم: مشاكل شائعة، أخطاء معروفة
```

**أنماط شائعة:**
```
"Cannot read property of undefined" → null check
"CORS error" → backend headers
"Module not found" → npm install / import path
"Connection refused" → server not running
"Permission denied" → chmod / sudo
```

---

## 🔄 للتنفيذ

### 9. Iterative Refinement
```
نسخة 1 (بسيطة) → اختبر → نسخة 2 (أفضل) → اختبر → ...

متى تُستخدم: أي ميزة جديدة
```

**مثال:**
```
ميزة: "بحث في المستخدمين"
↓
v1: بحث بالاسم فقط (يعمل!)
v2: + بحث بالإيميل
v3: + pagination
v4: + caching
```

---

### 10. Red-Green-Refactor (TDD)
```
Red: اكتب test يفشل
Green: اكتب أقل كود يُنجح الـ test
Refactor: نظّف الكود

متى تُستخدم: كود حرج، logic معقد
```

---

### 11. Spike & Stabilize
```
Spike: جرّب حل سريع وقذر (prototype)
Stabilize: إذا نجح، أعد كتابته بشكل نظيف

متى تُستخدم: تقنية جديدة، لست متأكداً من الحل
```

---

### 12. Breadcrumb Logging
```
أضف logs في كل خطوة → تتبع المسار → أزل بعد الإصلاح

متى تُستخدم: debug تدفق معقد
```

**مثال:**
```python
def process_order(order):
    print(f"[1] Received order: {order.id}")
    validated = validate(order)
    print(f"[2] Validated: {validated}")
    saved = save(order)
    print(f"[3] Saved: {saved}")
    notified = notify(order)
    print(f"[4] Notified: {notified}")
```

---

## 🎯 اختيار النمط المناسب

| المشكلة | النمط |
|---------|-------|
| لا أفهم الخطأ | Rubber Duck |
| الخطأ في مكان ما بملف كبير | Binary Search |
| مشكلة متكررة | 5 Whys |
| ميزة معقدة | Divide & Conquer |
| عدة احتمالات | Occam's Razor |
| تصميم جديد | First Principles |
| تصميم آمن | Inversion |
| مشكلة شائعة | Pattern Matching |
| ميزة جديدة | Iterative Refinement |
| تقنية جديدة | Spike & Stabilize |
| تدفق معقد | Breadcrumb Logging |
